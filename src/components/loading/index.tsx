import { Logo } from '@/assets/logo';
import { LoadingContainer } from '@/styles/pages/loading';

export const Loading = () => {
  return (
    <div>
      <LoadingContainer>
        <Logo />
      </LoadingContainer>
    </div>
  );
};
